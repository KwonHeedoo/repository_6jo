<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.scit6jo.web.dao.DataMapper">
	<insert id="insert" parameterType="interData">
		INSERT INTO (
			dataNum,
			userid,
			questionNum,
			answer,
			regdate,
			saveFile
		)
		VALUES(
			data_seq.nextval,
			#{userid},
			#{questionNum},
			#{answer},
			sysdate,
			#{saveFile}
		)
	
	</insert>

</mapper>
